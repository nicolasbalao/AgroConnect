<UserControl x:Class="Software.Views.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Software.Views"
             xmlns:vm="clr-namespace:Software.ViewModels"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:EmployeeViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                <StackPanel Margin="10">
                    <Label>Site filter</Label>
                    <ComboBox ItemsSource="{Binding Sites}" SelectedItem="{Binding SiteFilterSelected}" DisplayMemberPath="City" Style="{StaticResource MaterialDesignOutlinedComboBox}" Padding="10"/>
                </StackPanel>


                <StackPanel Margin="10">
                    <Label>Department filter</Label>
                    <ComboBox ItemsSource="{Binding Departments}" SelectedItem="{Binding DepartmentFilterSelected}" DisplayMemberPath="Name" Style="{StaticResource MaterialDesignOutlinedComboBox}" Padding="10"/>
                </StackPanel>

                <StackPanel Margin="10" Width="200">
                    <Label>Search</Label>
                    <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource MaterialDesignOutlinedTextBox}" Padding="10"/>
                </StackPanel>
            </StackPanel>

            
            <Button Command="{Binding CreateEmployeeCMD}" Visibility="{Binding GlobalState.IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Right">New</Button>


            <DataGrid ItemsSource="{Binding PaginatedEmployees.Items}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" SelectionChanged="DataGrid_SelectionChanged" Margin="0 50 0 0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Firstname" Binding="{Binding Firstname}"/>
                    <DataGridTextColumn Header="Lastname" Binding="{Binding Lastname}"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}"/>
                    <DataGridTextColumn Header="Phone number" Binding="{Binding CellPhone}"/>
                    <DataGridTextColumn Header="Home phone" Binding="{Binding HomePhone}"/>
                    <DataGridTextColumn Header="Department" Binding="{Binding Department.Name}"/>
                    <DataGridTextColumn Header="Site" Binding="{Binding Site.City}"/>
                </DataGrid.Columns>

            </DataGrid>

        </StackPanel>

    </Grid>

</UserControl>
